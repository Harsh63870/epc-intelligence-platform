import PlaceholderPage from "@/components/PlaceholderPage";

export default function SchedulePage() {
  return (
    <PlaceholderPage
      title="Schedule Risk"
      description="Predictive schedule risk engine analysing procurement status, lead times, and critical path dependencies."
    />
  );
}
